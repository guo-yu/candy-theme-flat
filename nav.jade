#navbar.nav-bar
  .container.clearfix
    .row
      .col-lg-4
        .sign.clearfix
          |欢迎回来，
          - if (!user)
            a(href="/sign") 请登录 #{site.name}
          - if (user)
            span.nickname #{user.nickname ? user.nickname : user.duoshuo.user_id}
      .col-lg-8
        - if (user)
          ul.nav-dashboard.clearfix
            - if (user.type == 'admin')
              li
                a(href="/admin")
                  i.fa.fa-gear
                  | 管理后台
            li
              a(href="/member/#{user._id}")
                i.fa.fa-home
                | 我的 #{site.name}
            li
              a(href="/thread/new")
                i.fa.fa-plus
                | 发表新话题
            li
              a(href="http://#{site.duoshuo.short_name}.duoshuo.com/logout/?sso=1&redirect_uri=#{url}/signout").signout 
                i.fa.fa-power-off
                | 登出
        - else
          .top-sign.clearfix
            .ds-login.clearfix
